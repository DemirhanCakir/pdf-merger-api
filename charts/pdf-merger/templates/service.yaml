apiVersion: v1
kind: Service
metadata:
  name: {{ include "pdf-merger.fullname" . }}
  labels:
    {{- include "pdf-merger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "pdf-merger.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
